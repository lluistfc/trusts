# Trusts model tests

Run `lua model_spec.lua` from this directory with Lua 5.1 or LuaJIT. The fixture covers hard functional requirements, directed player-to-Trust skillchains, and deterministic bounded team optimization. The modules are deliberately independent from Ashita so they can be tested outside the game.
